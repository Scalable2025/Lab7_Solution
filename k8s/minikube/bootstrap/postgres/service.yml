---
apiVersion: v1
kind: Service
metadata:
  name: postgres-customer
spec:
  selector:
    app: postgres-customer
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-fraud
spec:
  selector:
    app: postgres-fraud
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-notification
spec:
  selector:
    app: postgres-notification
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
